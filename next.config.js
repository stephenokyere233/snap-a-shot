/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["profile-assets.showwcase.com", "pbs.twimg.com"],
  },
};

module.exports = nextConfig
